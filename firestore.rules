rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // ログイン済みユーザーは自分自身の users/{uid} 以下のデータのみ読み書きできる
    match /users/{uid}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
